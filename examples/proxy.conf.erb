events { 
  worker_connections 1024;
}

error_log <%= error_log %>;
pid <%= pidfile %>;

http {
  access_log <%= access_log %>;

  <%= upstreams %>

  server {
    listen <%= address %>;

    <%= locations %>
  }
}
